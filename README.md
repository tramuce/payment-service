# payment-service

Para iniciar o projeto execute na raíz do projeto
* mvn clean install
* docker-compose up -d
* java -jar ./target/payment-service-0.0.1-SNAPSHOT.jar

Urls de acesso
* Eureka dashboard: http://localhost:8761/
* Swagger da aplicação: http://localhost:9000/swagger-ui.html
